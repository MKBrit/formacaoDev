// Online C++ compiler to run C++ program online
#include <iostream>
#include <locale>
using namespace std;

int max (int n1,int n2){   
    if  (n1 > n2)
        return n1;
    else if (n1 < n2)
        return n2;
}
void tabuada (int in){
    int out;
    for(int i=1;i<11;i++){
        out=in*i;
        cout << out << endl;
    }
}

int main() {
    setlocale(LC_ALL,"portuguese");
    int N1, N2; 
    cout << "Introduza 1º nr seguido por enter e outro nr " << endl;
    cin >> N1 >> N2;
    cout << "O maior nr é o " << max (N1,N2)<< endl;
    
    int input;
    cout << "Introduza nr a calcular a tabuada " << endl;
    cin >>input;
    cout << endl;
    tabuada(input);
    
    return 0;
}